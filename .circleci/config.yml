version: 2.1

executors:
  base:
    docker:
      - image: debian:stretch

workflows:
  schema-validations:
    jobs:
      - git-schema-validation
  expansion-testing:
    jobs:
      - git-expansion-testing
  runtime-testing:
    jobs:
      - git-runtime-testing
  integration-testing:
    jobs:
      - git-integration-testing

jobs:
  git-schema-validation:
    executor: base
    steps:
      - run: circleci orb validate src/git/orb.yml
  git-expansion-testing:
    executor: base
    steps:
      - run: echo 'Hello world!'
  git-runtime-testing:
    executor: base
    steps:
      - run: echo 'Hello world!'
  git-integration-testing:
    executor: base
    steps:
      - run: echo 'Hello world!'
